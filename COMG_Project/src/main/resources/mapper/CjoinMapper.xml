<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="kopo.poly.persistance.mapper.ICJoinMapper">

    <!-- 이메일(아이디)로 회원 가입 중복 방지하기 -->
    <select id="getUserExists" parameterType="CUserDTO" resultType="CUserDTO">
        SELECT
            (CASE WHEN COUNT(1) THEN 'Y' ELSE 'N' END) AS EXISTS_YN
        FROM USER_INFO
        WHERE USER_ID=#{user_id}
    </select>

    <insert id="InsertUserInfo" parameterType="CUserDTO">
        INSERT INTO USER_INFO(
            USER_ID,
            USER_PASSWORD,
            USER_NAME,
            STUDENT_ID,
            REG_ID,
            CHG_ID,
            FILE_SEQ
        ) values(
                    #{user_id},
                    #{user_password},
                    #{user_name},
                    #{student_id},
                    #{user_id},
                    #{user_id},
                    #{file_seq}
                )
    </insert>

    <update id="UpdateUserInfo" parameterType="CUserDTO">
         UPDATE USER_INFO
         SET FILE_SEQ = #{file_seq}
         WHERE USER_ID = #{user_id}
    </update>
</mapper>